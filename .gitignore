node_modules/

# Secrets — never commit these to a public repo.
# All .env variants are ignored except the checked-in template.
.env
.env.*
!.env.example

# Logs
*.log

# Runtime persistence — keep the folder, ignore its contents
# (these files hold member IDs and must not be published)
data/*.json
!data/.gitkeep
